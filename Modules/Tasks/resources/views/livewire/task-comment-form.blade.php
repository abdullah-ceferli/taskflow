<div><form wire:submit="save" class="mt-4"><textarea wire:model="body" rows="3" class="w-full rounded-xl border border-slate-300 p-3" placeholder="Add a comment"></textarea>@error('body')<p class="mt-2 text-sm text-rose-600">{{ $message }}</p>@enderror<button wire:loading.attr="disabled" class="mt-2 rounded-xl bg-indigo-600 px-4 py-2 text-sm font-semibold text-white disabled:opacity-50">Comment</button></form><div class="mt-5 space-y-4">@forelse($comments as $comment)<div class="rounded-xl bg-slate-50 p-4"><p class="text-sm font-semibold">{{ $comment->user->name ?: $comment->user->email }}</p><p class="mt-1 whitespace-pre-line text-sm text-slate-700">{{ $comment->body }}</p></div>@empty<p class="text-sm text-slate-500">No comments yet.</p>@endforelse</div></div>