<section class="mt-7 rounded-2xl border bg-white p-6 shadow-sm"><div class="flex flex-wrap items-center justify-between gap-3"><div><h3 class="font-semibold">Planning</h3><p class="mt-1 text-sm text-slate-500">Milestones, board workflow, and recurring delivery.</p></div><div class="flex gap-2"><a href="{{ route('projects.board', $project) }}" class="rounded-xl bg-slate-900 px-4 py-2 text-sm font-semibold text-white">Open board</a><a href="{{ route('projects.recurring-tasks.index', $project) }}" class="rounded-xl border px-4 py-2 text-sm font-semibold">Recurring tasks</a></div></div><div class="mt-5 grid gap-5 lg:grid-cols-[1.5fr_1fr]"><div class="space-y-3">@forelse($milestones as $milestone)<article class="rounded-xl bg-slate-50 p-4"><div class="flex items-start justify-between gap-3"><div><p class="font-semibold">{{ $milestone->name }}</p><p class="mt-1 text-xs text-slate-500">Due {{ $milestone->due_at->format('M j, Y') }} · {{ $milestone->done_tasks_count }}/{{ $milestone->tasks_count }} tasks</p></div>@if($milestone->at_risk)<span class="rounded-full bg-rose-100 px-2 py-1 text-xs font-bold text-rose-700">At risk</span>@elseif($milestone->completed_at)<span class="rounded-full bg-emerald-100 px-2 py-1 text-xs font-bold text-emerald-700">Complete</span>@endif</div><div class="mt-3 h-2 overflow-hidden rounded-full bg-slate-200"><div class="h-full rounded-full bg-indigo-500" style="width: {{ $milestone->progress }}%"></div></div>@can('update', $project) @unless($milestone->completed_at)<form method="POST" action="{{ route('projects.milestones.complete', [$project, $milestone]) }}" class="mt-3">@csrf @method('PATCH')<button class="text-xs font-semibold text-emerald-700">Mark complete</button></form>@endunless @endcan</article>@empty<p class="text-sm text-slate-500">No milestones yet.</p>@endforelse</div>@can('update', $project)<form method="POST" action="{{ route('projects.milestones.store', $project) }}" class="space-y-3 rounded-xl border p-4">@csrf<h4 class="text-sm font-semibold">New milestone</h4><input name="name" required placeholder="Milestone name" class="w-full rounded-lg border px-3 py-2 text-sm"><input type="date" name="due_at" required class="w-full rounded-lg border px-3 py-2 text-sm"><textarea name="description" rows="2" placeholder="Optional description" class="w-full rounded-lg border px-3 py-2 text-sm"></textarea><button class="rounded-lg bg-indigo-600 px-3 py-2 text-sm font-semibold text-white">Add milestone</button></form>@endcan</div></section>
