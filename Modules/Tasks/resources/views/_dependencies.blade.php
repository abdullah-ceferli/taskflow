<section class="mt-7 rounded-2xl border bg-white p-6 shadow-sm"><h3 class="font-semibold">Dependencies</h3><div class="mt-4 grid gap-5 md:grid-cols-2"><div><p class="text-xs font-bold uppercase tracking-widest text-slate-400">Blocked by</p><div class="mt-3 space-y-2">@forelse($task->dependencies as $dependency)<div class="flex items-center justify-between rounded-xl bg-slate-50 p-3 text-sm"><a href="{{ route('tasks.show', $dependency) }}" class="font-semibold text-indigo-700">{{ $dependency->number }} · {{ $dependency->title }}</a>@can('update', $task)<form method="POST" action="{{ route('tasks.dependencies.destroy', [$task, $dependency]) }}">@csrf @method('DELETE')<button class="text-xs font-semibold text-rose-600">Remove</button></form>@endcan</div>@empty<p class="text-sm text-slate-500">This task is not blocked.</p>@endforelse</div></div><div><p class="text-xs font-bold uppercase tracking-widest text-slate-400">Blocking</p><div class="mt-3 space-y-2">@forelse($task->dependents as $dependent)<a href="{{ route('tasks.show', $dependent) }}" class="block rounded-xl bg-slate-50 p-3 text-sm font-semibold text-indigo-700">{{ $dependent->number }} · {{ $dependent->title }}</a>@empty<p class="text-sm text-slate-500">No dependent tasks.</p>@endforelse</div></div></div>@can('update', $task)<form method="POST" action="{{ route('tasks.dependencies.store', $task) }}" class="mt-5 flex flex-wrap gap-2">@csrf<select name="depends_on_task_id" required class="min-w-64 flex-1 rounded-xl border px-3 py-2.5 text-sm"><option value="">Select dependency</option>@foreach($dependencyCandidates as $candidate)<option value="{{ $candidate->id }}">{{ $candidate->number }} · {{ $candidate->title }}</option>@endforeach</select><button class="rounded-xl bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white">Add dependency</button></form>@endcan</section>
